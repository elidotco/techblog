const { getPostLike } = require("../controllers/likeController");

const router = require("express").Router();

router.post;
router.get("/blog_likes", getPostLike);
router.get;

module.exports = router;
